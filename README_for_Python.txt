In Python 2: type 

import MIMOChannel

You can call the function MIMOChannel4x4  which takes a single numpy array (4 x
n), where n is the length of the transmission, as its input and returns a numpy
array of the same dimensions
